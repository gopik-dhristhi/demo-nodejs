If we have to push a tagand then while releasing it on new getting issue as this 
    "echo "Tag is not from main branch. Exiting."   
"
then run :-
    git fetch origin                        
    git checkout main
    git reset --hard origin/main



